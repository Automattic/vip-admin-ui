import { Controls, Meta, Source } from '@storybook/addon-docs/blocks';
import { Button } from '@wordpress/components';

import guide from '../../../docs/actions.md?raw';
import { ActionRow } from '../../../src/actions/ActionRow';
import * as ActionRowStories from '../../../src/actions/ActionRow.stories';
import { DoDont, GuideSection, PageNav, Recipe } from '../../docs/blocks';

<Meta title="Actions/Build" />

# Actions

**Status:** Beta&emsp;**Last updated:** 2026-09-22

<PageNav current="build" />

## Designing with it

<GuideSection guide={guide} section="order-and-alignment-actionrow" demote />

<DoDont
  do={ActionRowStories.DestructiveFirst}
  doCaption="Place a destructive action leftmost when the group has a safe primary."
  dont={
    <ActionRow>
      <Button variant="tertiary">Cancel</Button>
      <Button variant="primary">Save</Button>
      <Button variant="tertiary" isDestructive>Delete</Button>
    </ActionRow>
  }
  dontCaption="Do not place it rightmost, where the user expects the primary."
/>

<GuideSection guide={guide} section="size" demote />

<GuideSection guide={guide} section="icons" demote />

<GuideSection guide={guide} section="vocabulary" demote />

<GuideSection guide={guide} section="destructive-confirmation" demote />

### Before you ship

The component does not supply these. Specify them in the design:

- Mark the primary action, or mark that the group has none.
- Mark every irreversible action, and write its confirm's title, message and verb.
- Write the accessible name for every icon-only button. The tooltip shows the same text.

## Coding with it

### Import

<Source language="js" code="import { ActionRow } from '@automattic/vip-admin-ui';" dark />

<Recipe of={ActionRowStories.Default} caption="Dismiss first, primary last." />

### Props

<Controls of={ActionRowStories.Default} />

The table does not show the following:

- `ActionRow` takes `children`, `stretch` and `className`, and passes no other prop on.

### Recipes

#### In a narrow column

<Recipe of={ActionRowStories.Stretch} caption="In a column narrower than about 280px, every button stretches. The primary is lowest." />

#### In a card footer

<Recipe of={ActionRowStories.CardFooter} caption="Card footers use small buttons." />

#### With an icon

<Recipe of={ActionRowStories.WithIcon} caption={<>Pass the icon as <code>icon</code>, not in the label.</>} />
